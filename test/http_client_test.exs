defmodule GraffitiRemoval.HTTPClientTest do
  use ExUnit.Case, async: true
  doctest GraffitiRemoval
  

 # test http client: 2 tests
    # get_removal_requests_by_ward_and_date(ward, date) do
    # get_ward_by_alderman(alderman) do
    # make_fetch_request(alderman) do
    # make_fetch_request(ward, date) do
    # parse_ward(response) do
    # parse_ward_and_date(response) do
    # get_fields_from_request(data) do

end