defmodule GraffitiRemovalRequestFetcherTest do
  use ExUnit.Case
  doctest GraffitiRemovalRequestFetcher

  test "greets the world" do
    assert GraffitiRemovalRequestFetcher.hello() == :world
  end
end
